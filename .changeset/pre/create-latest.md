---
"create-bounda": patch
"@bounda-dev/react-router": patch
---

Document `npm create bounda@latest`. Without the tag, npx can reuse a `create-bounda` it cached
earlier, and that older scaffolder pins the Bounda packages to the version it shipped with.
